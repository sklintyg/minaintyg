apiVersion: v1
data:
  CERTIFICATE_BASEURL: "http://${intygstjanst.test.minaintyg.test.service.host}:8080"
  NTJP_WS_CERTIFICATE_TYPE: "JKS"
  NTJP_WS_TRUSTSTORE_TYPE: "JKS"
kind: ConfigMap
metadata:
  name: minaintyg-test-configmap-envvar
type: Opaque
