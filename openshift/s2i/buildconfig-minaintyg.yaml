apiVersion: v1
kind: BuildConfig
metadata:
  labels:
    app: minaintyg
  name: minaintyg
spec:
  output:
    to:
      kind: ImageStreamTag
      name: minaintyg:latest
  resources: {}
  source:
    git:
      ref: develop
      uri: https://github.com/sklintyg/minaintyg.git
    contextDir: /
    type: Git
  strategy:
    sourceStrategy:
      env:
      - name: commonVersion
        value: 3.6.0.+
      - name: infraVersion
        value: 3.6.0.+
      - name: buildVersion
        value: 3.6.0.0-OPENSHIFT
      from:
        kind: "ImageStreamTag"
        name: "intyg-s2i:latest"
      forcePull: true

