---
# This playbook deploys Mina Intyg 

- hosts: minaintyg-tomcat
  sudo: yes 
  vars: 
    # repo - values: snapshot or release
    repo: release
    version: 2.2.0-RC4
    config_version: release/v2.2.0

  roles:
    - minaintyg

