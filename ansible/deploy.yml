---
# This playbook deploys Mina Intyg 

- hosts: tomcat
  sudo: yes 
  vars: 
    # repo - values: snapshot or release
    repo: release

    # latest release: 2.1.0-b24
    # latest snapshot: 2.1.0-a664
    version: 2.1.0-b24
    config_version: master

  roles:
    - minaintyg
